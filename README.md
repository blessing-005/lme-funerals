# lme-funerals

Production-quality client-review build. Run `./START_REVIEW.ps1` for local preview, `./BUILD.ps1` to build, `./QA.ps1` for validation, and `./DEPLOY_REVIEW.ps1` to update GitHub Pages. Review mode uses `noindex,nofollow`.
